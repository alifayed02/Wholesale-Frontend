import { Call } from '../types';

export async function getDashboardData(): Promise<Call[]> {
  // This function is disabled for the dummy data MVP.
  // Ali will handle the backend connection later.
  return [];
} 